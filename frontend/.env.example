# Build-time DEFAULT coordinator (API) URL.
#
# This is only the fallback. The desktop app can point at any coordinator at
# runtime (Settings -> Connection, or the first-run setup screen); that choice
# is stored per-device and overrides this value. So you usually only need this
# for the web build / local dev.
VITE_API_URL=http://localhost:5000
